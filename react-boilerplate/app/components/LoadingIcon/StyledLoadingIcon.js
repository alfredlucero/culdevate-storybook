import styled from 'styled-components';

import { loadingIconWrapperStyle, loadingIconSquareStyle } from './loadingIconStyles';

const StyledLoadingIconWrapper = styled.div`${loadingIconWrapperStyle}`;

const StyledLoadingIconSquare = styled.div`${loadingIconSquareStyle}`;

export { StyledLoadingIconWrapper, StyledLoadingIconSquare };
